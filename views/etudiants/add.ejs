<!DOCTYPE html>
<html>
<head>
  <title>Ajouter Étudiant</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f8f9fa;
      color: #333;
      line-height: 1.6;
      padding: 20px;
    }
    
    .form-container {
      max-width: 600px;
      margin: 30px auto;
      padding: 30px;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    }
    
    h1 {
      color: #2c3e50;
      font-weight: 500;
      margin-bottom: 25px;
      text-align: center;
      font-size: 1.8rem;
    }
    
    .form-group {
      margin-bottom: 20px;
    }
    
    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 500;
      color: #495057;
    }
    
    input[type="text"],
    input[type="email"] {
      width: 100%;
      padding: 12px 15px;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      font-size: 1rem;
      transition: all 0.3s;
      box-sizing: border-box;
    }
    
    input[type="text"]:focus,
    input[type="email"]:focus {
      outline: none;
      border-color: #3a506b;
      box-shadow: 0 0 0 3px rgba(58, 80, 107, 0.1);
    }
    
    .btn-submit {
      width: 100%;
      background: #3a506b;
      color: white;
      border: none;
      padding: 14px;
      font-size: 1rem;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 500;
      transition: all 0.3s;
      margin-top: 10px;
    }
    
    .btn-submit:hover {
      background: #2c3e50;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    
    /* Style pour les champs obligatoires */
    input:required {
      border-left: 3px solid #5bc0be;
    }
    
    /* Style pour les champs facultatifs */
    input:optional {
      border-left: 3px solid #e0e0e0;
    }
  </style>
</head>
<body>
  <%- include('../partials/menu') %>

  <div class="form-container">
    <h1>Ajouter un Nouvel Étudiant</h1>
    
    <form action="/etudiants" method="POST">
      <div class="form-group">
        <label for="student_id">ID Étudiant:</label>
        <input type="text" id="student_id" name="student_id" required placeholder="Saisir l'ID étudiant">
      </div>
      
      <div class="form-group">
        <label for="first_name">Prénom:</label>
        <input type="text" id="first_name" name="first_name" required placeholder="Saisir le prénom">
      </div>
      
      <div class="form-group">
        <label for="last_name">Nom:</label>
        <input type="text" id="last_name" name="last_name" required placeholder="Saisir le nom">
      </div>
      
      <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required placeholder="exemple@email.com">
      </div>
      
      <div class="form-group">
        <label for="phone">Téléphone:</label>
        <input type="text" id="phone" name="phone" placeholder="Saisir le numéro de téléphone">
      </div>
      
      <button type="submit" class="btn-submit">Enregistrer l'étudiant</button>
    </form>
  </div>
</body>
</html>